{
  "name": "afrikanadollz-tools",
  "private": true,
  "version": "1.0.0",
  "description": "Local asset-prep tooling (NOT deployed to Azure Functions -- deliberately isolated from the root package.json so heavy native deps like onnxruntime-node/sharp never get zipped up as part of the Functions app deployment unit).",
  "main": "remove-background.js",
  "scripts": {
    "remove-background": "node remove-background.js"
  },
  "dependencies": {
    "@imgly/background-removal-node": "^1.4.5"
  }
}
